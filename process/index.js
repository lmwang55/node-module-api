/**
 * @param {参数相关} - argv argv0 execArgv execPath
 * 可获取运行指令时传入的参数 通常用 argv，从第三个参数开始。
 */

/**
 * @param {环境相关} - env
 */

/**
 * @param - cwd()
 * 打印当前命令所执行的路径。
 */

/**
 * @param {当前队列最后} - nextTick()
 * @param {介于之间} -setTimeout() | setInterval()
 * @param {下一队列开头(建议)} - setImmediate()
 */
