/**
 * @param {和路径有关的一切} path
 * @param {格式化路径} normalize()
 * @param {拼接路径} join()
 * @param {相对路径解析成绝对路径} resoleve()
 * @param {文件名} basename()
 * @param {路径} dirname()
 * @param {扩展名} extname()
 * @param {针对一个路径字符串返回一个路径对象} parse()
 * @param {针对一个路径对象返回一个路径字符串} format()
 * @param {路径分隔符} sep()
 * @param {path的分隔符} delimiter()
 * @param {属性：选择操作系统} win32 // win32.sep()
 * @param {属性：选择操作系统} posix // win32.delimiter()
 */
